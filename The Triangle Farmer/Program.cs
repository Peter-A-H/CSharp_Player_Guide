Console.WriteLine("Enter the triangle's base size: ");
int baseSize = Convert.ToInt32(Console.ReadLine());

Console.WriteLine("Enter the triangle's base height: ");
int baseHeight = Convert.ToInt32(Console.ReadLine());

int area = baseSize * baseHeight / 2;

Console.WriteLine("The area of the triangle is " + area);
